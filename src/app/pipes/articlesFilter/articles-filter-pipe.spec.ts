import { ArticlesFilterPipe } from './articles-filter.pipe';

describe('ArticlesFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticlesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
