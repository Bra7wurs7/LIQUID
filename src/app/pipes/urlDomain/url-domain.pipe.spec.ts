import { UrlDomainPipe } from './url-domain.pipe';

describe('UrlDomainPipe', () => {
  it('create an instance', () => {
    const pipe = new UrlDomainPipe();
    expect(pipe).toBeTruthy();
  });
});
