import { MapToListPipe } from './map-to-list.pipe';

describe('MapToListPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToListPipe();
    expect(pipe).toBeTruthy();
  });
});
