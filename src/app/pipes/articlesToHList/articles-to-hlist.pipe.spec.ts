import { ArticlesToHListPipe } from './articles-to-hlist.pipe';

describe('ArticlesToHListPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticlesToHListPipe();
    expect(pipe).toBeTruthy();
  });
});
