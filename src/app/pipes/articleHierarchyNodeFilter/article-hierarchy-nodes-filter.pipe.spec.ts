import { ArticleHierarchyNodesFilterPipe } from './article-hierarchy-nodes-filter.pipe';

describe('ArticleHierarchyNodesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleHierarchyNodesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
