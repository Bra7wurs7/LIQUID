import { ParseMarkupPipe } from './parse-markup.pipe';

describe('ParseMarkupPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseMarkupPipe();
    expect(pipe).toBeTruthy();
  });
});
