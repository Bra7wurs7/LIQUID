import { WorkspaceTooltipPipe } from './workspace-tooltip.pipe';

describe('WorkspaceTooltipPipe', () => {
  it('create an instance', () => {
    const pipe = new WorkspaceTooltipPipe();
    expect(pipe).toBeTruthy();
  });
});
