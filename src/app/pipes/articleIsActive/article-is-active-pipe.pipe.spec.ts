import { ArticleIsActivePipe } from './article-is-active-pipe.pipe';

describe('ArticleIsActivePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ArticleIsActivePipe();
    expect(pipe).toBeTruthy();
  });
});
